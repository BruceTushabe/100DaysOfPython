import numpy as np

vector_row = np.array([1,2,3])

# create a vector column

vector_column = np.array([[1], [2], [3]])


# create a matrix

matrix = np.array([[1,2], [1,3], [2,4]])


print("The vector row is: " +  str(vector_row) + " Vector column is " + str(vector_column) + " and the matrix is " + str(matrix))
